#############################################
# Exemplo de variáveis de ambiente (.env)
# Copie para .env e preencha valores reais.
#############################################

# Ambiente (development | staging | production)
ENVIRONMENT=development

# SMTP (Hostinger exemplo)
SMTP_SERVER=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=seu_usuario@seudominio.com
SMTP_PASSWORD=sua_senha_ou_app_password
SMTP_FROM=seu_usuario@seudominio.com

# JWT / Segurança (exemplos - substitua por segredos reais)
SECRET_KEY=trocar_por_um_valor_seguro
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Twilio (opcional para telefone/WhatsApp)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Outros opcionais
PORT=8001

# Observações:
# - Nunca commitar .env com segredos reais.
# - Para staging/produção use painel do provedor para definir variáveis.
# - Se SMTP_PORT=587 pode optar por STARTTLS.# ===================================================================
# Variáveis de Ambiente - Sistema Financeiro Pessoal
# ===================================================================
# Este é um arquivo de exemplo. Copie para .env e ajuste conforme necessário.
# NUNCA commite o arquivo .env real no Git!
# ===================================================================

# --- Banco de Dados ---
# Caminho do banco SQLite (relativo ao container)
DB_PATH=/app/data/lancamentos.db

# --- Ambiente ---
# production | development | testing
ENVIRONMENT=production

# --- Logs ---
# debug | info | warning | error | critical
LOG_LEVEL=info

# --- Servidor ---
# Host e porta (dentro do container)
HOST=0.0.0.0
PORT=8000

# --- Segurança ---
# IMPORTANTE: Gere uma chave secreta forte para produção!
# Exemplo: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE-THIS-IN-PRODUCTION-USE-STRONG-RANDOM-KEY
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=1440

# --- Backup ---
# Habilitar backups automáticos
AUTO_BACKUP_ENABLED=true
# Intervalo em horas
AUTO_BACKUP_INTERVAL_HOURS=24
# Manter últimos N backups
AUTO_BACKUP_KEEP_LAST=30

# --- SMTP (Verificação de E-mail) ---
# Exemplo Gmail
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASSWORD=senha_de_app_gmail  # NUNCA usar senha normal
# SMTP_FROM=seu-email@gmail.com

# Exemplo Outlook/Hotmail
# SMTP_SERVER=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=seu-email@outlook.com
# SMTP_PASSWORD=senha_de_app_outlook
# SMTP_FROM=seu-email@outlook.com

# Em development, se SMTP não configurado, o código aparece no console.

# --- PWA ---
# URL base da aplicação (para PWA)
APP_BASE_URL=http://localhost:8001

# --- Limites ---
# Tamanho máximo de upload (MB)
MAX_UPLOAD_SIZE_MB=10
# Limite de requisições por minuto (rate limiting - futuro)
RATE_LIMIT_PER_MINUTE=60

# --- Features ---
# Habilitar/desabilitar funcionalidades
ENABLE_PDF_EXPORT=true
ENABLE_EXCEL_EXPORT=true
ENABLE_NOTIFICATIONS=true
ENABLE_PWA=true

# --- Debug ---
# Mostrar detalhes de erro no frontend
DEBUG_MODE=false
# SQL debug (mostrar queries)
SQL_DEBUG=false
